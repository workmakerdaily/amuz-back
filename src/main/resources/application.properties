spring.application.name=todo-back

server.port=4000

jwt.secret=QZzj5kzY9tFz3aXqO6DwbP5P3J82bM1qgKlMzYvXNuY=

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/amuz?serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=root

# server.port=${PORT:8080}

# jwt.secret={JWT.SECRET}

# MySQL 연결 설정 (Railway 환경 변수)
# spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?serverTimezone=UTC&useSSL=false
# spring.datasource.username=${MYSQLUSER}
# spring.datasource.password=${MYSQLPASSWORD}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA 설정
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
# spring.jpa.hibernate.ddl-auto=none
# spring.jpa.show-sql=true